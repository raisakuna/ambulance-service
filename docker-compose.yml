version: '3.8'
services:
  sk-ambulance-service:
    image: sakuna-image  # Specify the desired image name and tag
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sakuna-service-container
    networks:
      - vastika-network
    restart: always
    ports:
      - '9091:8080'



networks:
  vastika-network:
    external: true
